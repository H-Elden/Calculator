# Calculator

本项目是一个开箱即用的使用**控制台窗口**快速高精度计算中缀表达式的程序。

## 特性

1. 内置数学常数
   - 圆周率 $\pi$: `Pi`
   - 自然常数 $e$: `e`
2. 有理数结果采用**分数**和**小数**同时输出
3. 多运算符支持：加减乘除 `+-*/`，指数 `^`，阶乘 `!`
4. 智能处理运算符优先级
5. 表达式错误智能提示：保证程序的稳定性

## 其他命令

`exit`: 退出程序

`clear`: 清空屏幕
